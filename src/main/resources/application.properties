spring.config.import=optional:file:./src/main/resources/env.properties
spring.application.name=grupo5

jwt.secret=${JWT_SECRET:GCJrc9*tnU(Y73E@SD&VIHqN}

spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://${DB_DATABASE:localhost:3306/ambientese}?createDatabaseIfNotExist=true&serverTimezone=UTC
spring.datasource.username=${DB_USERNAME:root}
spring.datasource.password=${DB_PASSWORD:root}
spring.jpa.hibernate.ddl-auto=update

# Change to true if you want to see the SQL queries generated by Hibernate
spring.jpa.show-sql=false

spring.mail.username=${EMAIL:your_email@example.com}
spring.mail.properties.mail.smtp.starttls.required=true
spring.mail.host=${EMAIL_HOST:smtp.example.com}
spring.mail.port=${EMAIL_PORT:587}
spring.mail.password=${EMAIL_APP_PASSWORD:your_email_app_password}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.transport.protocol=smtp
spring.mail.properties.mail.smtp.starttls.enable=true